"""query cartItems($amount: Int!) {
  products: productsRandomSuggestions(amount: $amount) {
    ...ProductCardFields
    variants {
      size
      material
      color {
        name
      }
    }
  }
}"""
